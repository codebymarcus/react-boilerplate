import React from 'react';
import PropTypes from 'prop-types';
import Main from 'Components/layouts/Main';

const Home = (props) => {
  return <Main center>Home Page</Main>;
};

Home.propTypes = {};

export default Home;
