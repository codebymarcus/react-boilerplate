import React from 'react';
import PropTypes from 'prop-types';
import Main from 'Components/layouts/Main';

const Login = (props) => {
  return <Main center>Login Page</Main>;
};

Login.propTypes = {};

export default Login;
